Description=start ${NAME}

[Service]
Type=simple
RemainAfterExit=yes
ExecStart=${APP}

# uncomment if used as a system service
# User=${USER}
# Group=${GROUP}

[Install]
WantedBy=default.target

# uncomment if used as a system service
# WantedBy=multi-user.target
