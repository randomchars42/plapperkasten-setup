# make single pins / lines / events accessible
KERNEL=="event*", SUBSYSTEM=="input", GROUP="${GROUP}", MODE="660"
# make the gpiochips accessibel
KERNEL=="gpiochip*", SUBSYSTEM=="gpio", GROUP="${GROUP}", MODE="660"
